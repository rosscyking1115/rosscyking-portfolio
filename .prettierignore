.next
out
build
node_modules
coverage
playwright-report
test-results
.vercel
.env*
pnpm-lock.yaml
package-lock.json

# Google Search Console verification file — must remain byte-identical.
public/google*.html

# Migration: the Astro app is a self-contained project with its own toolchain.
# Remove this when astro/ is promoted to the repo root.
astro
