# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production
# ...but keep the documented template. The ROOT .gitignore has a bare `.env*`
# which matches at every depth, so this negation is what makes it trackable.
!.env.example

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/
.vercel
