# npm needs --legacy-peer-deps because @vercel/analytics declares SvelteKit
# as a peerOptional, which makes npm 9+'s strict resolver chase Vite 8 even
# though we use the Next.js adapter. We're only using @vercel/analytics with
# Next.js — the SvelteKit branch is irrelevant.
legacy-peer-deps=true
